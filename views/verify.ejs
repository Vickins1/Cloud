<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify Your Email - Cloud 420</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(145deg, #1a1a1a, #035925);
            font-family: 'Arial', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            overflow-x: hidden;
            color: #e0e0e0;
        }
        .verify-container {
            background: rgba(26, 26, 26, 0.95);
            padding: 2.5rem;
            border-radius: 20px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.7), 0 0 20px rgba(3, 89, 37, 0.3);
            max-width: 500px;
            width: 100%;
            text-align: center;
            animation: fadeInUp 0.8s ease-out;
            border: 1px solid #035925;
        }
        .verify-container h1 {
            color: #1eff47;
            font-size: 2.2rem;
            margin-bottom: 1.5rem;
            font-weight: 700;
            text-shadow: 0 2px 5px rgba(0, 0, 0, 0.5);
        }
        .verify-container p {
            font-size: 1.1rem;
            color: #b0b0b0;
            margin-bottom: 2rem;
            line-height: 1.6;
        }
        .verify-container p strong {
            color: #fff;
            font-weight: 600;
        }
        .form-control {
            background: #f8f4f4;
            border: 2px solid #035925;
            color: #e0e0e0;
            padding: 0.75rem;
            font-size: 1rem;
            border-radius: 10px;
            transition: all 0.3s ease;
        }
        .form-control:focus {
            border-color: #1eff47;
            box-shadow: 0 0 15px rgba(30, 255, 71, 0.4);
            background: #fbfbfb;
            color: #000000;
        }
        .btn-resend {
            background: linear-gradient(90deg, #035925, #046d34);
            color: #fff;
            padding: 0.85rem 2rem;
            border-radius: 25px;
            font-size: 1.1rem;
            font-weight: 600;
            text-transform: uppercase;
            border: none;
            transition: all 0.3s ease;
        }
        .btn-resend:hover {
            background: linear-gradient(90deg, #028e39, #05a042);
            box-shadow: 0 5px 20px rgba(30, 255, 71, 0.5);
            transform: translateY(-2px);
        }
        .alert {
            margin-bottom: 1.5rem;
            padding: 0.75rem;
            font-size: 0.95rem;
            border-radius: 10px;
            opacity: 0.9;
            animation: fadeIn 0.5s ease-in;
        }
        .alert-danger {
            background: #4a1a1a;
            border-color: #721c24;
            color: #f8d7da;
        }
        .alert-success {
            background: #1a4a1a;
            border-color: #2a722a;
            color: #d4edda;
        }
        a {
            color: #56cfe1;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }
        a:hover {
            color: #80e9ff;
            text-decoration: underline;
        }
        /* Animations */
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 0.9; }
        }
        /* Responsive Design */
        @media (max-width: 576px) {
            .verify-container {
                padding: 1.5rem;
                margin: 1rem;
                max-width: 90%;
            }
            h1 {
                font-size: 1.8rem;
                margin-bottom: 1rem;
            }
            p {
                font-size: 1rem;
            }
            .form-control {
                padding: 0.65rem;
                font-size: 0.9rem;
            }
            .btn-resend {
                padding: 0.75rem 1.5rem;
                font-size: 1rem;
            }
            .alert {
                font-size: 0.9rem;
                padding: 0.65rem;
            }
        }
    </style>
</head>
<body>
    <div class="verify-container">
        <h1>Verify Your Email</h1>
        <% if (typeof error_msg !== 'undefined' && error_msg.length > 0) { %>
            <div class="alert alert-danger text-center"><%= error_msg %></div>
        <% } %>
        <% if (typeof success_msg !== 'undefined' && success_msg.length > 0) { %>
            <div class="alert alert-success text-center"><%= success_msg %></div>
        <% } %>
        <p>We’ve sent a verification link to <strong><%= email || 'your email' %></strong>. Please check your inbox (and spam/junk folder) to activate your Cloud 420 account.</p>
        <form action="/auth/resend-verification" method="POST">
            <div class="mb-3">
                <input type="email" name="email" class="form-control" placeholder="Enter your email" value="<%= email %>" required>
            </div>
            <button type="submit" class="btn btn-resend">Resend Verification Email</button>
        </form>
        <p class="mt-3">Already verified? <a href="/auth/login">Log in here</a>.</p>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>