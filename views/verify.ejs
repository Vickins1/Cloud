<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        body {
            min-height: 100vh;
            background: linear-gradient(to bottom, #1a1a1a, #000000); /* Dark gradient */
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .container {
            max-width: 450px;
            width: 100%;
            background: #000000; /* Dark gray background */
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.4); /* Deeper shadow */
            overflow: hidden;
            border: 1px solid #3a3a3a; /* Subtle border */
        }

        .header {
            background: linear-gradient(135deg, #1e4d28, #2e6b3a); /* Darker green gradient */
            padding: 20px;
            text-align: center;
            color: #e0e0e0; /* Light gray text */
        }

        .header img {
            max-width: 80px;
            margin-bottom: 10px;
            filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.5)); /* Shadow for logo */
            transition: transform 0.3s ease;
        }

        .header img:hover {
            transform: scale(1.1); /* Subtle hover effect */
        }

        .header h1 {
            font-size: 26px;
            font-weight: 600;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        .content {
            padding: 30px;
        }

        .message-box {
            margin-bottom: 20px;
            padding: 12px;
            border-radius: 8px;
            text-align: center;
            font-size: 14px;
            color: #fff;
        }

        .success {
            background: rgba(30, 77, 40, 0.9); /* Semi-transparent dark green */
            border: 1px solid #2e6b3a;
        }

        .error {
            background: rgba(139, 0, 0, 0.9); /* Semi-transparent dark red */
            border: 1px solid #b22222;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 20px; /* Increased gap for better spacing */
        }

        .input-group {
            position: relative;
        }

        label {
            font-size: 14px;
            color: #a0a0a0; /* Light gray for labels */
            margin-bottom: 8px;
            display: block;
            font-weight: 500;
        }

        input[type="email"],
        input[type="text"] {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #3a3a3a; /* Darker border */
            border-radius: 25px;
            font-size: 14px;
            background: #1f1f1f; /* Dark input background */
            color: #e0e0e0; /* Light text */
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

        input:focus {
            outline: none;
            border-color: #2e6b3a; /* Highlight with dark green */
            box-shadow: 0 0 8px rgba(46, 107, 58, 0.5); /* Glowing effect */
        }

        input::placeholder {
            color: #6a6a6a; /* Muted placeholder */
        }

        button {
            padding: 12px 25px;
            background: linear-gradient(135deg, #1e4d28, #2e6b3a); /* Gradient button */
            color: #e0e0e0;
            border: none;
            border-radius: 25px;
            font-weight: 600;
            font-size: 14px;
            text-transform: uppercase;
            cursor: pointer;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        button:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(46, 107, 58, 0.6); /* Green glow */
            background: linear-gradient(135deg, #2e6b3a, #3a8a4b); /* Lighter on hover */
        }

        .footer {
            text-align: center;
            padding: 15px;
            font-size: 12px;
            color: #a0a0a0; /* Light gray */
            background: #2e2e2e; /* Slightly darker footer */
            border-top: 1px solid #3a3a3a;
        }

        .footer a {
            color: #2e6b3a; /* Dark green link */
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .footer a:hover {
            color: #3a8a4b; /* Lighter green on hover */
        }

        @media (max-width: 480px) {
            .container { 
                margin: 10px; 
                border-radius: 12px; 
            }
            .header h1 { 
                font-size: 22px; 
            }
            .content { 
                padding: 20px; 
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <img src="/images/Cloud.png" alt="Cloud 420 Logo">
            <h1>Verify Your Email</h1>
        </div>
        <div class="content">
            <% if (success) { %>
                <div class="message-box success"><%= success %></div>
            <% } %>
            <% if (error) { %>
                <div class="message-box error"><%= error %></div>
            <% } %>
            <form action="/auth/verify" method="POST">
                
                <div class="input-group">
                    <label for="code">Verification Code</label>
                    <input 
                        type="text" 
                        id="code" 
                        name="code" 
                        placeholder="Enter your code" 
                        required
                    >
                </div>
                <button type="submit">Verify Email</button>
            </form>
        </div>
        <div class="footer">
            <p>© <%= new Date().getFullYear() %> Cloud 420 • Crafted with 🌿 • 
                <a href="/auth/login">Back to Login</a>
            </p>
        </div>
    </div>
</body>
</html>