<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment</title>
    <link rel="stylesheet" href="/path/to/your/styles.css"> 
    <link rel="icon" href="/images/icon.png" >
    <style>
        /* Inline CSS for demonstration. Move these styles to your CSS file for production. */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            color: #333;
            background-color: #f8f9fa;
        }

        header {
            background: #035925; /* Dark green background */
            color: white;
            padding: 20px;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        header h1 {
            margin: 0;
            font-size: 2.5rem;
        }

        main {
            padding: 20px;
            max-width: 600px;
            margin: 0 auto;
        }

        .form-group {
            margin-bottom: 15px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        .form-control {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .btn-primary {
            background-color: #035925; /* Dark green */
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1rem;
        }

        .btn-primary:hover {
            background-color: #023d1a; /* Darker green */
        }

        .form-control[readonly] {
            background-color: #e9ecef;
        }
    </style>
</head>
<body>
    <header>
        <h1>Payment Gateway</h1>
        <p>Your secure payment portal</p>
        <!-- You can add navigation links or other header content here if needed -->
    </header>
    <main>
        <h2>Initiate Payment</h2>
        <form id="payment-form" action="http://localhost:4200/payment/confirm" method="POST">
            <div class="form-group">
                <label for="amount">Amount (KES):</label>
                <input type="number" id="amount" name="amount" class="form-control" value="<%= cartTotal %>" readonly required>
            </div>
            <div class="form-group">
                <label for="msisdn">Phone Number:</label>
                <input type="text" id="msisdn" name="msisdn" class="form-control" required>
            </div>
            <div class="form-group">
                <label for="reference">Reference (optional):</label>
                <input type="text" id="reference" name="reference" class="form-control">
            </div>
            <button type="submit" class="btn-primary">Pay</button>
        </form>
        
        
    </main>
</body>
</html>
