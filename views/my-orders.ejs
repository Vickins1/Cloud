<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Galactic Orders | Cloud420</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="icon" href="/images/icon.png">
    <style>
        body {
            background: linear-gradient(to bottom, #0a0a0a, #031a03);
            color: #d4d4d4;
            font-family: 'Roboto Mono', monospace;
            min-height: 100vh;
            margin: 0;
            padding: 0;
        }
        .cosmic-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg"><circle cx="5" cy="5" r="1" fill="rgba(3,89,37,0.3)"/></svg>') repeat;
            opacity: 0.6;
            z-index: -1;
            animation: starfield 100s linear infinite;
        }
        @keyframes starfield {
            0% { background-position: 0 0; }
            100% { background-position: 1000px 1000px; }
        }
        .cosmic-container {
            max-width: 1100px;
            margin: 100px auto 50px;
            padding: 30px;
            background: rgba(10, 10, 10, 0.95);
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(3, 89, 37, 0.5);
        }
        .navbar-dark {
            background: #0a0a0a;
            box-shadow: 0 2px 15px rgba(3, 89, 37, 0.3);
            padding: 15px 0;
        }
        .navbar-brand img {
            height: 40px;
            transition: transform 0.3s;
        }
        .navbar-brand:hover img {
            transform: scale(1.05);
        }
        .nav-link {
            color: #d4d4d4 !important;
            font-family: 'Orbitron', sans-serif;
            font-size: 1rem;
            padding: 8px 15px;
            transition: color 0.3s;
        }
        .nav-link:hover {
            color: #3cb371 !important;
        }
        .header {
            background: linear-gradient(135deg, #0a0a0a, #031a03);
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            color: #fff;
            font-family: 'Orbitron', sans-serif;
            margin-bottom: 30px;
        }
        .header h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin: 0;
            text-shadow: 0 0 10px rgba(3, 89, 37, 0.7);
        }
        .header p {
            font-size: 1.1rem;
            color: #a9a9a9;
            margin: 5px 0 0;
        }
        .order-orb {
            background: #151515;
            border: 1px solid #3cb371;
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
            transition: box-shadow 0.3s;
        }
        .order-orb:hover {
            box-shadow: 0 0 15px rgba(3, 89, 37, 0.6);
        }
        .order-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-bottom: 15px;
            border-bottom: 1px solid #3cb371;
        }
        .order-header h5 {
            margin: 0;
            font-family: 'Orbitron', sans-serif;
            font-size: 1.3rem;
            color: #fff;
        }
        .badge {
            font-family: 'Roboto Mono', monospace;
            font-size: 0.9rem;
            padding: 6px 12px;
            border-radius: 20px;
        }
        .items-list {
            list-style: none;
            padding: 0;
            margin: 15px 0;
        }
        .items-list li {
            display: flex;
            justify-content: space-between;
            padding: 10px 0;
            border-bottom: 1px solid rgba(60, 179, 113, 0.2);
            font-size: 1rem;
            color: #d4d4d4;
        }
        .items-list li span:first-child {
            font-weight: 500;
        }
        .total, .ordered {
            font-size: 1.1rem;
            color: #fff;
            margin: 10px 0 0;
        }
        .pagination .page-link {
            background: #3cb371;
            color: #fff;
            border: none;
            font-family: 'Orbitron', sans-serif;
            padding: 8px 12px;
            margin: 0 5px;
            border-radius: 5px;
            transition: background 0.3s;
        }
        .pagination .page-item.active .page-link {
            background: #2e8b57;
        }
        .pagination .page-link:hover {
            background: #2e8b57;
            color: #fff;
        }
        .no-orders {
            text-align: center;
            padding: 40px;
            color: #a9a9a9;
            font-family: 'Orbitron', sans-serif;
        }
        .no-orders .btn {
            background: #3cb371;
            border: none;
            padding: 10px 20px;
            font-size: 1.2rem;
            border-radius: 25px;
            color: #fff;
            transition: background 0.3s, transform 0.3s;
        }
        .no-orders .btn:hover {
            background: #2e8b57;
            transform: translateY(-3px);
        }
        .alert {
            border-radius: 10px;
            margin-bottom: 20px;
        }
        .alert-danger {
            background: #2d0000;
            border-color: #5a0d0d;
            color: #d4d4d4;
        }
        .alert-success {
            background: #002d0a;
            border-color: #3cb371;
            color: #d4d4d4;
        }
        .alert .close {
            color: #d4d4d4;
            opacity: 0.7;
        }

        /* Responsive Adjustments */
        @media (max-width: 992px) {
            .cosmic-container {
                margin: 80px 20px 40px;
                padding: 20px;
            }
            .header h1 {
                font-size: 2rem;
            }
            .order-header {
                flex-direction: column;
                align-items: flex-start;
            }
            .order-header h5 {
                font-size: 1.2rem;
                margin-bottom: 10px;
            }
        }
        @media (max-width: 768px) {
            .cosmic-container {
                margin: 60px 15px 30px;
                padding: 15px;
            }
            .navbar-brand img {
                height: 35px;
            }
            .nav-link {
                font-size: 0.9rem;
            }
            .header h1 {
                font-size: 1.8rem;
            }
            .order-orb {
                padding: 15px;
            }
        }
        @media (max-width: 576px) {
            .cosmic-container {
                margin: 50px 10px 20px;
                padding: 10px;
            }
            .navbar-brand img {
                height: 30px;
            }
            .header h1 {
                font-size: 1.5rem;
            }
            .order-header h5 {
                font-size: 1.1rem;
            }
            .items-list li {
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <div class="cosmic-bg"></div>
    <div class="cosmic-container">
        <header>
            <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
                <div class="container">
                    <a class="navbar-brand" href="/">
                        <img src="/images/Cloud.png" alt="Cloud 420 Store Logo">
                    </a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarNav">
                        <ul class="navbar-nav ms-auto">
                            <li class="nav-item"><a class="nav-link" href="/products"><i class="fas fa-leaf"></i> Dispensary</a></li>
                            <li class="nav-item">
                                <a class="nav-link" href="/cart"><i class="fas fa-shopping-cart"></i> Cart
                                    <% if (cartItems > 0) { %>
                                        <span class="badge bg-success" id="cart-count"><%= cartItems %></span>
                                    <% } %>
                                </a>
                            </li>
                            <% if (isAuthenticated) { %>
                                <li class="nav-item"><a class="nav-link" href="/home"><i class="fas fa-home"></i> Home</a></li>
                                <li class="nav-item"><a class="nav-link" href="/cart/my-orders"><i class="fas fa-box-open"></i> My Orders</a></li>
                                <li class="nav-item"><a class="nav-link" href="/profile"><i class="fas fa-user"></i> Profile</a></li>
                                <li class="nav-item"><a class="nav-link" href="/auth/logout"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
                            <% } else { %>
                                <li class="nav-item"><a class="nav-link" href="/auth/login"><i class="fas fa-sign-in-alt"></i> Login</a></li>
                                <li class="nav-item"><a class="nav-link" href="/auth/signup"><i class="fas fa-user-plus"></i> Sign Up</a></li>
                            <% } %>
                        </ul>
                    </div>
                </div>
            </nav>
        </header>
        <div class="header">
            <h1>My Galactic Orders</h1>
            <p>Track your orders seamlessly across the cosmos</p>
        </div>

        <% if (error_msg && error_msg.length > 0) { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <%= error_msg[0] %>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
        <% } %>
        <% if (success_msg && success_msg.length > 0) { %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <%= success_msg[0] %>
                <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
        <% } %>

        <% if (orders.length > 0) { %>
            <% orders.forEach(order => { %>
                <div class="order-orb">
                    <div class="order-header">
                        <h5>Order #<%= order.orderId.toString().slice(-6) %></h5>
                        <div>
                            <span class="badge <%= order.status === 'completed' ? 'bg-success' : order.status === 'failed' ? 'bg-danger' : 'bg-warning' %>">
                                <%= order.cosmicStatus %>
                            </span>
                            <span class="badge bg-info ms-2"><%= order.shippingStatus %></span>
                        </div>
                    </div>
                    <ul class="items-list">
                        <% order.items.forEach(item => { %>
                            <li title="<%= item.cosmicDesc %>">
                                <span><%= item.name %></span>
                                <span>x<%= item.quantity %> - KES <%= (item.price * item.quantity).toFixed(2).replace(/\d(?=(\d{3})+\.)/g, '$&,') %></span>
                            </li>
                        <% }); %>
                    </ul>
                    <p class="total">Total: <strong>KES <%= order.total %></strong></p>
                    <p class="ordered">Ordered: <%= new Date(order.createdAt).toLocaleString('en-US', { timeZone: 'Africa/Nairobi' }) %></p>
                </div>
            <% }); %>

            <nav aria-label="Order navigation">
                <ul class="pagination justify-content-center mt-4">
                    <% if (currentPage > 1) { %>
                        <li class="page-item">
                            <a class="page-link" href="/my-orders?page=<%= currentPage - 1 %>">Previous</a>
                        </li>
                    <% } %>
                    <% for (let i = 1; i <= totalPages; i++) { %>
                        <li class="page-item <%= currentPage === i ? 'active' : '' %>">
                            <a class="page-link" href="/my-orders?page=<%= i %>"><%= i %></a>
                        </li>
                    <% } %>
                    <% if (currentPage < totalPages) { %>
                        <li class="page-item">
                            <a class="page-link" href="/my-orders?page=<%= currentPage + 1 %>">Next</a>
                        </li>
                    <% } %>
                </ul>
            </nav>
        <% } else { %>
            <div class="no-orders">
                <p>No orders found in your galaxy.</p>
                <a href="/products" class="btn">Start Shopping</a>
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>