<!DOCTYPE html>
<html lang="en">

<head>
       <meta charset="utf-8">
       <meta name="viewport" content="width=device-width, initial-scale=1">
       <title>Cloud 420 | Admin Dashboard</title>

       <!-- Google Font: Source Sans Pro -->
       <link rel="stylesheet"
              href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,400i,700&display=fallback">
       <!-- Font Awesome Icons -->
       <link rel="stylesheet" href="/plugins/fontawesome-free/css/all.min.css">
       <!-- overlayScrollbars -->
       <link rel="stylesheet" href="/plugins/overlayScrollbars/css/OverlayScrollbars.min.css">
       <!-- Theme style -->
       <link rel="stylesheet" href="/dist/css/adminlte.min2167.css?v=3.2.0">
       <link rel="icon" type="image/png" sizes="64x64" href="/images/icon.png">
</head>

<body class="hold-transition dark-mode sidebar-mini layout-fixed layout-navbar-fixed layout-footer-fixed">
       <div class="wrapper">

              <!-- Preloader -->
              <div class="preloader flex-column justify-content-center align-items-center">
                     <img class="animation__wobble" src="/images/icon.png" alt="AdminLTELogo" height="200"
                            width="200">
              </div>

              <!-- Navbar -->
              <nav class="main-header navbar navbar-expand navbar-dark">
                     <!-- Left navbar links -->
                     <ul class="navbar-nav">
                            <li class="nav-item">
                                   <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i
                                                 class="fas fa-bars"></i></a>
                            </li>
                            <li class="nav-item d-none d-sm-inline-block">
                                   <a href="/" class="nav-link">Home</a>
                            </li>
                     </ul>

                     <!-- Right navbar links -->
                     <ul class="navbar-nav ml-auto">
                           

                            <li class="nav-item">
                                   <a class="nav-link" data-widget="fullscreen" href="#" role="button">
                                          <i class="fas fa-expand-arrows-alt"></i>
                                   </a>
                            </li>
                            <li class="nav-item">
                                   <a class="nav-link" data-widget="control-sidebar" data-slide="true" href="#"
                                          role="button">
                                          <i class="fas fa-th-large"></i>
                                   </a>
                            </li>
                     </ul>
              </nav>

             <!-- Main Sidebar Container -->
        <aside class="main-sidebar sidebar-dark-primary elevation-4">
              <!-- Brand Logo -->
              <a href="/admin/dashboard" class="brand-link">
                  <img src="/images/Cloud.png" alt="Cloud 420 Logo" class="brand-image img-circle elevation-3" style="opacity: .8">
                  <span class="brand-text font-weight-light"></span>
              </a>
  
              <!-- Sidebar -->
              <div class="sidebar">
                  <div class="user-panel mt-3 pb-3 mb-3 d-flex">
                      <div class="image">
                          <img src="/dist/img/user2-160x160.jpg" class="img-circle elevation-2" alt="User Image">
                      </div>
                      <div class="info">
                          <a href="#" class="d-block"><strong><%= currentUser.username %></strong></a>
                      </div>
                  </div>
  
                  <nav class="mt-2">
                    <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
                        <!-- Dashboard -->
                        <li class="nav-item menu-open">
                            <a href="/admin/dashboard" class="nav-link active">
                                <i class="nav-icon fas fa-tachometer-alt"></i>
                                <p>Dashboard</p>
                            </a>
                        </li>
                
                        <!-- Products -->
                        <li class="nav-item">
                            <a href="/admin/products" class="nav-link">
                                <i class="nav-icon fas fa-cube"></i>
                                <p>Products</p>
                            </a>
                        </li>
                
                        <!-- Orders -->
                        <li class="nav-item">
                            <a href="/admin/orders" class="nav-link">
                                <i class="nav-icon fas fa-shopping-bag"></i>
                                <p>Orders</p>
                            </a>
                        </li>
                
                        <!-- Carts (optional) -->
                        <li class="nav-item">
                            <a href="/admin/carts" class="nav-link">
                                <i class="nav-icon fas fa-shopping-cart"></i>
                                <p>Carts</p>
                            </a>
                        </li>
                
                        <!-- Transactions -->
                        <li class="nav-item">
                            <a href="/admin/transactions" class="nav-link">
                                <i class="nav-icon fas fa-exchange-alt"></i>
                                <p>Transactions</p>
                            </a>
                        </li>
                
                        <!-- Users -->
                        <li class="nav-item">
                            <a href="/admin/users" class="nav-link">
                                <i class="nav-icon fas fa-users"></i>
                                <p>Users</p>
                            </a>
                        </li>
                
                        <!-- Communications (Comms) -->
                        <li class="nav-item">
                            <a href="/support/comms" class="nav-link">
                                <i class="nav-icon fas fa-envelope"></i>
                                <p>Communications</p>
                            </a>
                        </li>
                
                        <!-- Settings -->
                        <li class="nav-item">
                            <a href="/admin/settings" class="nav-link">
                                <i class="nav-icon fas fa-cogs"></i>
                                <p>Settings</p>
                            </a>
                        </li>
                
                        <!-- Logout -->
                        <li class="nav-item">
                            <a href="/admin/logout" class="nav-link">
                                <i class="nav-icon fas fa-sign-out-alt"></i>
                                <p>Logout</p>
                            </a>
                        </li>
                    </ul>
                </nav>
                     </div>
              </aside>

              <div class="content-wrapper">

            <section class="content">
                <div class="container-fluid">
                    <div class="card shadow-lg border-0">
                        <div class="card-header border-0 d-flex justify-content-between align-items-center py-2">
                            <h3 class="card-title text-white font-weight-bold m-0" style="font-size: 1.25rem;">Manage
                                Products</h3>
                            <div class="card-tools">
                                <button type="button" class="btn btn-sm btn-success shadow-sm" data-toggle="modal"
                                    data-target="#addProductModal">
                                    <i class="fas fa-plus mr-1"></i> Add Product
                                </button>
                            </div>
                        </div>
                        <div class="card-body p-0">
                            <% if (typeof error_msg !=='undefined' && error_msg.length> 0) { %>
                                <div class="alert alert-danger alert-dismissible fade show mx-2 mt-2" role="alert">
                                    <%= error_msg[0] %>
                                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                                            <span aria-hidden="true">×</span>
                                        </button>
                                </div>
                                <% } %>
                                    <% if (typeof success_msg !=='undefined' && success_msg.length> 0) { %>
                                        <div class="alert alert-success alert-dismissible fade show mx-2 mt-2"
                                            role="alert">
                                            <%= success_msg[0] %>
                                                <button type="button" class="close" data-dismiss="alert"
                                                    aria-label="Close">
                                                    <span aria-hidden="true">×</span>
                                                </button>
                                        </div>
                                        <% } %>

                                        <div class="table-responsive">
                                            <table class="table table-dark table-hover table-sm m-0">
                                                <thead>
                                                    <thead>
                                                        <tr>
                                                            <th class="py-2 pl-2">Name</th>
                                                            <th class="py-2">Price</th>
                                                            <th class="py-2">Stock</th>
                                                            <th class="py-2">Quantity Type</th>
                                                            <th class="py-2">Category</th>
                                                            <th class="py-2 pr-2">Actions</th>
                                                        </tr>
                                                    </thead>
                                                    <tbody>
                                                        <% products.forEach(product => { %>
                                                            <tr>
                                                                <td class="py-2 pl-2"><%= product.name %></td>
                                                                <td class="py-2">KES <%= product.price.toFixed(2) %></td>
                                                                <td class="py-2"><%= product.stockQuantity %></td>
                                                                <td class="py-2"><%= product.quantityType || 'N/A' %></td> <!-- Quantity type (grams, pieces, packs) -->
                                                                <td class="py-2"><%= product.category || 'N/A' %></td>
                                                                <td class="py-2 pr-2">
                                                                    <button class="btn btn-sm btn-warning" data-toggle="modal" data-target="#editProductModal-<%= product._id %>">
                                                                        Edit
                                                                    </button>
                                                                    <form action="/admin/products/delete/<%= product._id %>" method="POST" class="d-inline" onsubmit="return confirmDelete()">
                                                                        <button type="submit" class="btn btn-xs btn-danger">
                                                                            <i class="fas fa-trash-alt"></i>
                                                                        </button>
                                                                    </form>
                                                                </td>
                                                            </tr>
                                        
<!-- Edit Product Modal -->
<div class="modal fade" id="editProductModal-<%= product._id %>" tabindex="-1" role="dialog" aria-labelledby="editProductModalLabel-<%= product._id %>" aria-hidden="true">
    <div class="modal-dialog modal-md" role="document">
        <div class="modal-content bg-dark text-white shadow-sm" style="border-radius: 12px; border: 1px solid #ffc107;">
            <div class="modal-header border-0 py-2" style="background: linear-gradient(135deg, #1a1a1a, #856404);">
                <h5 class="modal-title font-weight-bold" id="editProductModalLabel-<%= product._id %>" style="font-size: 1.25rem; color: #fff3cd;">Edit Product</h5>
                <button type="button" class="close text-white" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true" style="font-size: 1.25rem;">×</span>
                </button>
            </div>
            <div class="modal-body p-3">
                <form action="/admin/products/edit" method="POST" enctype="multipart/form-data">
                    <input type="hidden" name="id" value="<%= product._id %>">
                    <div class="form-group mb-3">
                        <label class="text-muted font-weight-bold small">Product Name</label>
                        <input type="text" class="form-control bg-secondary text-white border-0 shadow-sm" name="name" value="<%= product.name %>" required style="border-radius: 8px; padding: 8px; font-size: 0.9rem;">
                    </div>
                    <div class="form-group mb-3">
                        <label class="text-muted font-weight-bold small">Price (KES)</label>
                        <input type="number" class="form-control bg-secondary text-white border-0 shadow-sm" name="price" step="0.01" value="<%= product.price %>" required style="border-radius: 8px; padding: 8px; font-size: 0.9rem;">
                    </div>
                    <div class="form-group mb-3">
                        <label class="text-muted font-weight-bold small">Description</label>
                        <textarea class="form-control bg-secondary text-white border-0 shadow-sm" name="description" rows="2" style="border-radius: 8px; padding: 8px; font-size: 0.9rem;"><%= product.description %></textarea>
                    </div>
                    <div class="form-group mb-3">
                        <label class="text-muted font-weight-bold small">Stock Quantity</label>
                        <input type="number" class="form-control bg-secondary text-white border-0 shadow-sm" name="stockQuantity" value="<%= product.stockQuantity %>" required style="border-radius: 8px; padding: 8px; font-size: 0.9rem;">
                    </div>
                    <div class="form-group mb-3">
                        <label class="text-muted font-weight-bold small">Quantity Type</label>
                        <select class="form-control bg-secondary text-white border-0 shadow-sm" name="quantityType" required style="border-radius: 8px; padding: 8px; font-size: 0.9rem;">
                            <option value="gram" <%= product.quantityType === 'gram' ? 'selected' : '' %>>Grams</option>
                            <option value="piece" <%= product.quantityType === 'piece' ? 'selected' : '' %>>Pieces</option>
                            <option value="pack" <%= product.quantityType === 'pack' ? 'selected' : '' %>>Packs</option>
                        </select>
                    </div>
                    <div class="form-group mb-3">
                        <label class="text-muted font-weight-bold small">Category</label>
                        <select class="form-control bg-secondary text-white border-0 shadow-sm" name="category" required style="border-radius: 8px; padding: 8px; font-size: 0.9rem;">
                            <option value="" disabled>Select a category</option>
                            <% const categories = [
                                { value: "Indica", label: "Indica 🌙" },
                                { value: "Sativa", label: "Sativa ☀️" },
                                { value: "Hybrid", label: "Hybrid 🔄" },
                                { value: "CBD", label: "CBD 🌿" },
                                { value: "THC", label: "High-THC 🔥" },
                                { value: "Exotic", label: "Exotic Strains 🏝️" },
                                { value: "Cigarettes", label: "Cigarettes 🚬" },
                                { value: "Cigars", label: "Cigars 🏆" },
                                { value: "Rolling-Tobacco", label: "Rolling Tobacco 🌀" },
                                { value: "Hookah", label: "Hookah/Shisha 💨" },
                                { value: "Nicotine-Pouches", label: "Nicotine Pouches 🏁" },
                                { value: "Vapes", label: "Vapes" },
                                { value: "Accessories", label: "Accessories" },
                                { value: "Edibles", label: "Edibles" }
                            ]; %>
                            <% categories.forEach(cat => { %>
                                <option value="<%= cat.value %>" <%= product.category === cat.value ? 'selected' : '' %>><%= cat.label %></option>
                            <% }); %>
                        </select>
                    </div>
                    <div class="form-group mb-3">
                        <label class="text-muted font-weight-bold small">Current Images</label>
                        <div class="d-flex flex-wrap gap-2 mb-2" id="currentImagesContainer-<%= product._id %>">
                            <% if (product.imageUrl) { %>
                                <div class="image-wrapper position-relative">
                                    <img src="<%= product.imageUrl %>" class="current-image" style="max-width: 80px; max-height: 80px; border-radius: 6px; object-fit: cover; margin: 5px;">
                                    <button type="button" class="btn btn-sm btn-danger remove-image" data-url="<%= product.imageUrl %>" data-product-id="<%= product._id %>">×</button>
                                </div>
                            <% } %>
                            <% if (product.additionalImages) { %>
                                <% product.additionalImages.forEach(img => { %>
                                    <div class="image-wrapper position-relative">
                                        <img src="<%= img %>" class="current-image" style="max-width: 80px; max-height: 80px; border-radius: 6px; object-fit: cover; margin: 5px;">
                                        <button type="button" class="btn btn-sm btn-danger remove-image" data-url="<%= img %>" data-product-id="<%= product._id %>">×</button>
                                    </div>
                                <% }); %>
                            <% } %>
                        </div>
                        <input type="hidden" id="removedImages-<%= product._id %>" name="removedImages">
                    </div>
                    <div class="form-group mb-3">
                        <label class="text-muted font-weight-bold small">Upload New Images (Optional)</label>
                        <input type="file" class="form-control-file" name="images" accept="image/*" multiple style="padding: 6px; border: 1px dashed #ffc107; border-radius: 8px; background: #1a1a1a; font-size: 0.85rem;">
                    </div>
                    <button type="submit" class="btn btn-warning btn-block shadow-sm" style="border-radius: 20px; padding: 8px; font-size: 0.9rem; font-weight: bold;">Update Product</button>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
document.addEventListener("DOMContentLoaded", function () {
    document.querySelectorAll(".remove-image").forEach(button => {
        button.addEventListener("click", function () {
            const productId = this.getAttribute("data-product-id");
            const removedImagesInput = document.getElementById("removedImages-" + productId);
            let removedImages = JSON.parse(removedImagesInput.value || "[]");

            const imageWrapper = this.parentElement;
            const imageUrl = this.getAttribute("data-url");

            removedImages.push(imageUrl);
            removedImagesInput.value = JSON.stringify(removedImages);

            imageWrapper.remove();
        });
    });
});
</script>

                                                    <% }); %>
                                                </tbody>
                                            </table>
                                        </div>
                                        
                        </div>
                    </div>

                   <!-- Pagination Controls -->
<nav>
    <ul class="pagination justify-content-center">
        <!-- Previous Button -->
        <% if (currentPage > 1) { %>
            <li class="page-item">
                <a class="page-link" href="?page=<%= currentPage - 1 %>">Previous</a>
            </li>
        <% } %>

        <!-- Page Numbers -->
        <% for (let i = 1; i <= totalPages; i++) { %>
            <li class="page-item <%= (currentPage === i) ? 'active' : '' %>">
                <a class="page-link" href="?page=<%= i %>"><%= i %></a>
            </li>
        <% } %>

        <!-- Next Button -->
        <% if (currentPage < totalPages) { %>
            <li class="page-item">
                <a class="page-link" href="?page=<%= currentPage + 1 %>">Next</a>
            </li>
        <% } %>
    </ul>
</nav>



<!-- Add Product Modal -->
<div class="modal fade" id="addProductModal" tabindex="-1" role="dialog" aria-labelledby="addProductModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-md" role="document">
        <div class="modal-content bg-dark text-white shadow-sm" style="border-radius: 10px; border: 1px solid #035925;">
            <div class="modal-header border-0 py-2" style="background: linear-gradient(135deg, #1a1a1a, #2d2d2d);">
                <h5 class="modal-title font-weight-bold" id="addProductModalLabel" style="font-size: 1.25rem; color: #d4edda;">Add New Product</h5>
                <button type="button" class="close text-white" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true" style="font-size: 1.25rem;">×</span>
                </button>
            </div>
            <div class="modal-body p-3">
                <form id="addProductForm" action="/admin/products/add" method="POST" enctype="multipart/form-data">
                    <div class="form-group mb-3">
                        <label for="name" class="text-muted font-weight-bold small">Product Name</label>
                        <input type="text" class="form-control bg-secondary text-white border-0 shadow-sm" id="name" name="name" required style="border-radius: 8px; padding: 8px; font-size: 0.9rem;">
                    </div>
                    <div class="form-group mb-3">
                        <label for="price" class="text-muted font-weight-bold small">Price (KES)</label>
                        <input type="number" class="form-control bg-secondary text-white border-0 shadow-sm" id="price" name="price" step="0.01" required style="border-radius: 8px; padding: 8px; font-size: 0.9rem;">
                    </div>
                    <div class="form-group mb-3">
                        <label for="description" class="text-muted font-weight-bold small">Description</label>
                        <textarea class="form-control bg-secondary text-white border-0 shadow-sm" id="description" name="description" rows="2" style="border-radius: 8px; padding: 8px; font-size: 0.9rem;"></textarea>
                    </div>
                    <div class="form-group mb-3">
                        <label for="stockQuantity" class="text-muted font-weight-bold small">Stock Quantity</label>
                        <input type="number" class="form-control bg-secondary text-white border-0 shadow-sm" id="stockQuantity" name="stockQuantity" required style="border-radius: 8px; padding: 8px; font-size: 0.9rem;">
                    </div>
                    <div class="form-group mb-3">
                        <label for="quantityType" class="text-muted font-weight-bold small">Quantity Type</label>
                        <select class="form-control bg-secondary text-white border-0 shadow-sm" id="quantityType" name="quantityType" required style="border-radius: 8px; padding: 8px; font-size: 0.9rem;">
                            <option value="gram">Grams</option>
                            <option value="piece">Pieces</option>
                            <option value="pack">Packs</option>
                        </select>
                    </div>
                    <div class="form-group mb-3">
                        <label for="category" class="text-muted font-weight-bold small">Category</label>
                        <select class="form-control bg-secondary text-white border-0 shadow-sm" id="category" name="category" required style="border-radius: 8px; padding: 8px; font-size: 0.9rem;">
                            <option value="Indica">Indica 🌙</option>
                            <option value="Sativa">Sativa ☀️</option>
                            <option value="Hybrid">Hybrid 🔄</option>
                            <option value="CBD">CBD 🌿</option>
                            <option value="THC">High-THC 🔥</option>
                            <option value="Exotic">Exotic Strains 🏝️</option>
                            <option value="Cigarettes">Cigarettes 🚬</option>
                            <option value="Cigars">Cigars 🏆</option>
                            <option value="Rolling-Tobacco">Rolling Tobacco 🌀</option>
                            <option value="Hookah">Hookah/Shisha 💨</option>
                            <option value="Nicotine-Pouches">Nicotine Pouches 🏁</option>
                            <option value="Vapes">Vapes</option>
                            <option value="Accessories">Accessories</option>
                            <option value="Edibles">Edibles</option>
                        </select>
                    </div>
                    <div class="form-group mb-3">
                        <label class="text-muted font-weight-bold small">Product Images (First image will be main)</label>
                        <input type="file" class="form-control-file" id="images" name="images" accept="image/*" multiple required style="padding: 6px; border: 1px dashed #035925; border-radius: 8px; background: #1a1a1a; font-size: 0.85rem;" onchange="previewImages(event, 'addImagePreview')">
                        <div id="addImagePreview" class="d-flex flex-wrap gap-2 mt-2"></div>
                        <small class="text-muted d-block mt-1" style="font-size: 0.75rem;">
                            Max 10 images, 5MB each. First image is used as the main image.
                        </small>
                    </div>
                    <button type="submit" class="btn btn-success btn-block shadow-sm" style="border-radius: 20px; padding: 8px; font-size: 0.9rem; font-weight: bold;">Add Product</button>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- Updated JavaScript -->
<script>
// Preview images (combined for all images)
function previewImages(event, previewId) {
    const input = event.target;
    const previewContainer = document.getElementById(previewId);
    previewContainer.innerHTML = ''; // Clear previous previews

    const files = input.files;
    if (!files || files.length === 0) return;

    if (files.length > 10) {
        alert('You can upload a maximum of 10 images.');
        input.value = '';
        return;
    }

    const maxSize = 5 * 1024 * 1024; // 5MB
    for (let i = 0; i < files.length; i++) {
        if (files[i].size > maxSize) {
            alert(`File "${files[i].name}" exceeds 5MB limit.`);
            input.value = '';
            previewContainer.innerHTML = '';
            return;
        }

        const reader = new FileReader();
        reader.onload = function (e) {
            const img = document.createElement('img');
            img.src = e.target.result;
            img.style.maxWidth = '80px';
            img.style.maxHeight = '80px';
            img.style.borderRadius = '6px';
            img.style.objectFit = 'cover';
            img.style.margin = '5px';
            previewContainer.appendChild(img);
        };
        reader.readAsDataURL(files[i]);
    }
}

// Keep your existing DOMContentLoaded logic if needed for other modals
document.addEventListener('DOMContentLoaded', function () {
    const fileInputs = document.querySelectorAll('input[type="file"]');
    fileInputs.forEach(input => {
        input.addEventListener('touchstart', function (e) {
            e.preventDefault();
            input.click();
        }, { passive: false });
    });
});
</script>
                </div>
            </section>
        </div>

        <!-- Control Sidebar -->
        <aside class="control-sidebar control-sidebar-dark">
        </aside>

        <!-- Main Footer -->
        <footer class="main-footer">
            <div class="float-right d-none d-sm-block">
                <b>Version</b> 1.0.1
            </div>
            <strong>Copyright © 2025 <a href="/">Cloud 420</a>.</strong>
            All rights reserved.
        </footer>
    </div>
    <script src="/js/jquery.min.js"></script> <!-- Assuming jQuery for Bootstrap -->
    <script src="/js/bootstrap.bundle.min.js"></script> <!-- Bootstrap JS -->
    <!-- REQUIRED SCRIPTS -->
    <script src="/plugins/jquery/jquery.min.js"></script>
    <script src="/plugins/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="/plugins/overlayScrollbars/js/jquery.overlayScrollbars.min.js"></script>
    <script src="/dist/js/adminlte2167.js?v=3.2.0"></script>
    <script src="/plugins/jquery-mousewheel/jquery.mousewheel.js"></script>
    <script src="/plugins/raphael/raphael.min.js"></script>
    <script src="/plugins/jquery-mapael/jquery.mapael.min.js"></script>
    <script src="/plugins/jquery-mapael/maps/usa_states.min.js"></script>
    <script src="/plugins/chart.js/Chart.min.js"></script>
    <script src="/dist/js/demo.js"></script>
    <script src="/dist/js/pages/dashboard2.js"></script>
</body>

</html>