<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Cloud 420 | Communications Hub</title>
    <!-- Google Font: Source Sans Pro -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,400i,700&display=fallback">
    <!-- Font Awesome Icons -->
    <link rel="stylesheet" href="/plugins/fontawesome-free/css/all.min.css">
    <!-- overlayScrollbars -->
    <link rel="stylesheet" href="/plugins/overlayScrollbars/css/OverlayScrollbars.min.css">
    <!-- Theme style -->
    <link rel="stylesheet" href="/dist/css/adminlte.min2167.css?v=3.2.0">
    <link rel="icon" type="image/png" sizes="64x64" href="/images/icon.png">

    <style>
        :root {
            --primary-bg: #1c2526;
            --secondary-bg: #2a363b;
            --glass-bg: rgba(255, 255, 255, 0.05);
            --text-color: #e0e0e0;
            --muted-text: #b0b0b0;
            --accent-color: #00c853;
            --hover-color: #00e676;
            --shadow: 0 8px 32px rgba(0, 200, 83, 0.2);
            --neumorph-shadow: 8px 8px 16px rgba(0, 0, 0, 0.3), -8px -8px 16px rgba(42, 54, 59, 0.1);
            --border-color: rgba(0, 200, 83, 0.3);
        }

        body {
            background: var(--primary-bg);
            color: var(--text-color);
            font-family: 'Source Sans Pro', sans-serif;
            overflow-x: hidden;
        }

        /* Navbar */
        .main-header.navbar {
            background: var(--secondary-bg);
            border-bottom: 1px solid var(--border-color);
            padding: 0.5rem 1rem;
        }
        .navbar-dark .navbar-nav .nav-link {
            color: var(--text-color);
            padding: 0.5rem 1rem;
        }
        .navbar-dark .navbar-nav .nav-link:hover,
        .navbar-dark .navbar-nav .nav-link:focus {
            color: var(--hover-color);
            background: var(--glass-bg);
        }

        /* Sidebar */
        .main-sidebar {
            background: var(--secondary-bg);
            border-right: 1px solid var(--border-color);
            transition: width 0.3s ease;
        }
        .sidebar-dark-primary .nav-sidebar .nav-link {
            color: var(--muted-text);
            padding: 0.75rem 1rem;
        }
        .sidebar-dark-primary .nav-sidebar .nav-link:hover,
        .sidebar-dark-primary .nav-sidebar .nav-link.active {
            color: var(--text-color);
            background: var(--accent-color);
            border-radius: 4px;
        }
        .brand-link {
            background: var(--secondary-bg);
            border-bottom: 1px solid var(--border-color);
            color: var(--text-color);
            padding: 1rem;
        }
        .user-panel {
            border-bottom: 1px solid var(--border-color);
            padding: 1rem;
        }
        .user-panel .info a {
            color: var(--text-color);
        }
        .user-panel .info a:hover {
            color: var(--hover-color);
        }

        /* Footer */
        .main-footer {
            background: var(--secondary-bg);
            color: var(--muted-text);
            border-top: 1px solid var(--border-color);
            padding: 1rem;
        }
        .main-footer a {
            color: var(--accent-color);
            transition: color 0.3s ease;
        }
        .main-footer a:hover {
            color: var(--hover-color);
        }

        /* Content Wrapper */
        .content-wrapper {
            padding: 1.5rem;
            min-height: calc(100vh - 70px);
            background: radial-gradient(circle at top left, rgba(0, 200, 83, 0.1), var(--primary-bg) 70%);
        }
        .comms-hub {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1.5rem;
        }
        .section-title {
            font-size: 2rem;
            color: var(--accent-color);
            text-align: center;
            margin-bottom: 1.5rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
        }

        /* Flash Messages */
        .flash-messages {
            position: fixed;
            top: 15px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 1000;
            width: 90%;
            max-width: 500px;
        }
        .flash-success, .flash-error {
            padding: 0.75rem 1.5rem;
            border-radius: 10px;
            backdrop-filter: blur(10px);
            color: var(--text-color);
            font-weight: 500;
            animation: slideDown 0.5s ease-out;
            margin-bottom: 0.75rem;
        }
        .flash-success {
            background: rgba(0, 200, 83, 0.9);
            box-shadow: var(--shadow);
        }
        .flash-error {
            background: rgba(244, 67, 54, 0.9);
            box-shadow: 0 8px 32px rgba(244, 67, 54, 0.2);
        }

        /* Support Section */
        .support-section {
            background: var(--secondary-bg);
            padding: 1.5rem;
            border-radius: 15px;
            box-shadow: var(--neumorph-shadow);
            backdrop-filter: blur(5px);
        }
        .accordion-item {
            background: var(--glass-bg);
            border-radius: 10px;
            overflow: hidden;
            border: 1px solid var(--border-color);
            margin-bottom: 0.75rem;
        }
        .accordion-header {
            padding: 1rem;
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: var(--glass-bg);
        }
        .accordion-header:hover {
            background: rgba(0, 200, 83, 0.2);
        }
        .accordion-header h3 {
            font-size: 1.1rem;
            color: var(--text-color);
            margin: 0;
        }
        .accordion-header i {
            color: var(--muted-text);
            transition: transform 0.3s ease;
        }
        .accordion-header.active i {
            transform: rotate(180deg);
            color: var(--accent-color);
        }
        .accordion-content {
            max-height: 0;
            padding: 0 1rem;
            overflow: hidden;
            transition: max-height 0.3s ease;
            background: var(--secondary-bg);
        }
        .accordion-content.active {
            max-height: 400px; /* Reduced for mobile */
            padding: 1rem;
        }
        .message-details p {
            margin: 0.5rem 0;
            color: var(--muted-text);
            font-size: 0.95rem;
        }
        .message-details p strong {
            color: var(--text-color);
        }
        .reply-form {
            margin-top: 1rem;
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
        }
        .reply-form textarea {
            background: var(--glass-bg);
            border: 1px solid var(--border-color);
            border-radius: 6px;
            padding: 0.75rem;
            color: var(--text-color);
            resize: none;
            height: 100px;
            width: 100%;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }
        .reply-form textarea:focus {
            border-color: var(--accent-color);
            box-shadow: 0 0 8px rgba(0, 200, 83, 0.5);
            outline: none;
        }
        .reply-form button {
            background: var(--accent-color);
            border: none;
            padding: 0.75rem;
            border-radius: 6px;
            color: var(--text-color);
            font-weight: bold;
            cursor: pointer;
            transition: background 0.3s ease, transform 0.3s ease;
        }
        .reply-form button:hover {
            background: var(--hover-color);
            transform: translateY(-2px);
        }

        /* Update Section */
        .update-section {
            background: var(--secondary-bg);
            padding: 1.5rem;
            border-radius: 15px;
            box-shadow: var(--neumorph-shadow);
            backdrop-filter: blur(5px);
        }
        .update-form {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
        .update-form input[type="text"],
        .update-form textarea {
            padding: 0.75rem;
            border-radius: 6px;
            border: 1px solid var(--border-color);
            background: var(--glass-bg);
            color: var(--text-color);
            font-size: 0.95rem;
            width: 100%;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }
        .update-form textarea {
            height: 150px;
            resize: vertical;
        }
        .update-form input[type="text"]:focus,
        .update-form textarea:focus {
            border-color: var(--accent-color);
            box-shadow: 0 0 8px rgba(0, 200, 83, 0.5);
            outline: none;
        }
        .update-form button {
            background: var(--accent-color);
            border: none;
            padding: 0.75rem;
            border-radius: 6px;
            color: var(--text-color);
            font-weight: bold;
            cursor: pointer;
            transition: background 0.3s ease, transform 0.3s ease;
        }
        .update-form button:hover {
            background: var(--hover-color);
            transform: translateY(-2px);
        }

        /* Animations */
        @keyframes slideDown {
            from { transform: translateY(-100px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        /* Responsive Design */
        @media (max-width: 1200px) {
            .comms-hub {
                grid-template-columns: 1fr;
                gap: 1rem;
            }
            .section-title {
                font-size: 1.8rem;
            }
        }
        @media (max-width: 768px) {
            .main-header.navbar {
                padding: 0.5rem;
            }
            .navbar-nav {
                flex-direction: row;
                flex-wrap: wrap;
            }
            .navbar-nav .nav-link {
                padding: 0.5rem 0.75rem;
            }
            .main-sidebar {
                width: 260px; /* Default AdminLTE width */
            }
            .content-wrapper {
                padding: 1rem;
            }
            .support-section, .update-section {
                padding: 1rem;
            }
            .accordion-header h3 {
                font-size: 1rem;
            }
            .reply-form textarea,
            .update-form textarea {
                height: 120px;
            }
            .message-details p {
                font-size: 0.9rem;
            }
        }
        @media (max-width: 480px) {
            .section-title {
                font-size: 1.5rem;
                letter-spacing: 1px;
            }
            .flash-messages {
                width: 95%;
                top: 10px;
            }
            .flash-success, .flash-error {
                padding: 0.5rem 1rem;
                font-size: 0.9rem;
            }
            .reply-form textarea,
            .update-form textarea {
                height: 100px;
            }
            .reply-form button,
            .update-form button {
                padding: 0.6rem;
                font-size: 0.9rem;
            }
            .accordion-content.active {
                max-height: 300px;
            }
            .main-footer {
                text-align: center;
                padding: 0.75rem;
            }
            .main-footer .float-right {
                float: none !important;
                margin-top: 0.5rem;
            }
        }
    </style>
</head>
<body class="hold-transition dark-mode sidebar-mini layout-fixed layout-navbar-fixed layout-footer-fixed">
    <div class="wrapper">
        <!-- Preloader -->
        <div class="preloader flex-column justify-content-center align-items-center">
            <img class="animation__wobble" src="/images/icon.png" alt="AdminLTELogo" height="200" width="200">
        </div>

        <!-- Navbar -->
        <nav class="main-header navbar navbar-expand navbar-dark">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
                </li>
                <li class="nav-item d-none d-sm-inline-block">
                    <a href="/" class="nav-link">Home</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" data-widget="fullscreen" href="#" role="button">
                        <i class="fas fa-expand-arrows-alt"></i>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" data-widget="control-sidebar" data-slide="true" href="#" role="button">
                        <i class="fas fa-th-large"></i>
                    </a>
                </li>
            </ul>
        </nav>

        <!-- Main Sidebar Container -->
        <aside class="main-sidebar sidebar-dark-primary elevation-4">
            <a href="/admin/dashboard" class="brand-link">
                <img src="/images/Cloud.png" alt="Cloud 420 Logo" class="brand-image img-circle elevation-3" style="opacity: .8">
                <span class="brand-text font-weight-light"></span>
            </a>
            <div class="sidebar">
                <div class="user-panel mt-3 pb-3 mb-3 d-flex">
                    <div class="image">
                        <img src="/dist/img/user2-160x160.jpg" class="img-circle elevation-2" alt="User Image">
                    </div>
                    <div class="info">
                        <a href="#" class="d-block"><strong><%= currentUser.username %></strong></a>
                    </div>
                </div>
                <nav class="mt-2">
                    <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
                        <li class="nav-item">
                            <a href="/admin/dashboard" class="nav-link">
                                <i class="nav-icon fas fa-tachometer-alt"></i>
                                <p>Dashboard</p>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="/admin/products" class="nav-link">
                                <i class="nav-icon fas fa-cube"></i>
                                <p>Products</p>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="/admin/orders" class="nav-link">
                                <i class="nav-icon fas fa-shopping-bag"></i>
                                <p>Orders</p>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="/admin/carts" class="nav-link">
                                <i class="nav-icon fas fa-shopping-cart"></i>
                                <p>Carts</p>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="/admin/transactions" class="nav-link">
                                <i class="nav-icon fas fa-exchange-alt"></i>
                                <p>Transactions</p>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="/admin/users" class="nav-link">
                                <i class="nav-icon fas fa-users"></i>
                                <p>Users</p>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="/support/comms" class="nav-link active">
                                <i class="nav-icon fas fa-envelope"></i>
                                <p>Communications</p>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="/admin/settings" class="nav-link">
                                <i class="nav-icon fas fa-cogs"></i>
                                <p>Settings</p>
                            </a>
                        </li>
                        <li class="nav-item">
                            <a href="/admin/logout" class="nav-link">
                                <i class="nav-icon fas fa-sign-out-alt"></i>
                                <p>Logout</p>
                            </a>
                        </li>
                    </ul>
                </nav>
            </div>
        </aside>

        <!-- Content Wrapper -->
        <div class="content-wrapper">
            <div class="comms-hub">
                <!-- Flash Messages -->
                <div class="flash-messages">
                    <% if (success_msg && success_msg.length > 0) { %>
                        <div class="flash-success"><%= success_msg %></div>
                    <% } %>
                    <% if (error_msg && error_msg.length > 0) { %>
                        <div class="flash-error"><%= error_msg %></div>
                    <% } %>
                </div>

                <!-- Support Section -->
                <section class="support-section">
                    <h2 class="section-title">Support Inbox</h2>
                    <div class="accordion">
                        <% messages.forEach(msg => { %>
                            <div class="accordion-item">
                                <div class="accordion-header" onclick="toggleAccordion(this)">
                                    <h3><%= msg.name %> - #<%= msg.orderNumber || 'N/A' %></h3>
                                    <i class="fas fa-chevron-down"></i>
                                </div>
                                <div class="accordion-content">
                                    <div class="message-details">
                                        <p><strong>Message:</strong> <%= msg.message %></p>
                                        <p><strong>Status:</strong> <%= msg.status %></p>
                                        <% if (msg.reply) { %>
                                            <p><strong>Reply:</strong> <%= msg.reply %></p>
                                        <% } else { %>
                                            <form class="reply-form" action="/support/reply" method="POST">
                                                <input type="hidden" name="messageId" value="<%= msg._id %>">
                                                <textarea name="reply" placeholder="Type your professional response..." required></textarea>
                                                <button type="submit">Send Reply</button>
                                            </form>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                </section>

                <!-- Update Section -->
                <section class="update-section">
                    <h2 class="section-title">Broadcast Updates</h2>
                    <form class="update-form" action="/admin/comms/send-update" method="POST">
                        <input type="text" name="subject" placeholder="Update Subject" required>
                        <textarea name="message" placeholder="Craft your update here..." required></textarea>
                        <button type="submit">Send to All Users</button>
                    </form>
                </section>
            </div>
        </div>

        <!-- Footer -->
        <footer class="main-footer">
            <div class="float-right d-none d-sm-block">
                <b>Version</b> 1.0.1
            </div>
            <strong>Copyright © 2025 <a href="/">Cloud 420</a>.</strong> All rights reserved.
        </footer>
    </div>

    <!-- Scripts -->
    <script src="/plugins/jquery/jquery.min.js"></script>
    <script src="/plugins/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="/dist/js/adminlte2167.js?v=3.2.0"></script>
    <script>
        function toggleAccordion(element) {
            const content = element.nextElementSibling;
            const icon = element.querySelector('i');
            element.classList.toggle('active');
            content.classList.toggle('active');
        }
    </script>
</body>
</html>